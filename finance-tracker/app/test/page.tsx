// I don't know what exactly is causing this but routes are built based on folder structure; the following page
// will show up if you add /test to the url
const TestPage = () => {
  return <div>Test Page</div>;
};

export default TestPage;
